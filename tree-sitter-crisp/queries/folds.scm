; CRISP Code Folding
; ==================

; Fold zones
(data_zone) @fold
(prose_zone) @fold
(table_zone) @fold
(list_zone) @fold
(raw_zone) @fold
(seq_zone) @fold

; Fold seq entries
(seq_entry) @fold

; Fold arrays
(array) @fold

; Fold inline tables
(inline_table) @fold

; Fold table sections
(table_header) @fold
(array_table_header) @fold

; Fold multiline strings
(multiline_basic_string) @fold
(multiline_literal_string) @fold

; Fold preamble
(preamble) @fold
